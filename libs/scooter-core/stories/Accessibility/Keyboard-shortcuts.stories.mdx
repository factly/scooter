import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { ScooterCore } from "@factly/scooter-core";
import {
  EDITOR_SHORTCUTS_TABLE_COLUMNS,
  EDITOR_SHORTCUTS_TABLE_ROWS,
} from "../constants";
import Table from "../components/Table";

<Meta
  title="Accessibility/Keyboard shortcuts"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={ScooterCore}
/>

# Keyboard shortcuts

Here is a list of keyboard shortcuts available in scooterEditor.

<Table
  columns={EDITOR_SHORTCUTS_TABLE_COLUMNS}
  rows={EDITOR_SHORTCUTS_TABLE_ROWS}
/>

Refer
[ https://tiptap.dev/api/keyboard-shortcuts](https://tiptap.dev/api/keyboard-shortcuts)
for the full list of available shortcuts.

<br />

### **Custom Keyboard shortcuts**

By default, scooterEditor submits the content on pressing the `⌘ + Enter` in Mac
or `Ctrl + Enter` in Windows. An `onSubmit` prop can be provided to call a
function when the content is submitted. It accepts the resulting HTML content as
argument.

Additional keyboard shortcuts can be created by using the `keyboardShortcuts`
prop. It accepts an object. The key is the key combination to trigger the
action, and value is the function to be called when the key combination is
pressed. You can use `Mod` as a shorthand for `Cmd` on Mac and `Control` on
other platforms. For instance, `Mod-Enter` corresponds to _Cmd+Enter_ on Mac and
_Control+Enter_ on other platforms. Remember to return `true` to prevent the
default behavior of the key combination. Keyboard shortcuts are now only
supported in the rich-text mode.

<Canvas>
  <Story name="Custom Keyboard shortcuts">
    <ScooterCore
      onSubmit={content => console.log(content)}
      keyboardShortcuts={{
        "Shift-Enter": ({ editor }) => {
          alert(editor.getHTML());
          return true;
        },
      }}
    />
  </Story>
</Canvas>
