import { EmbedExtension } from "@factly/scooter-embed";
import { EmbedFetcher } from "@factly/scooter-embed";

export const Embed = {
  extensionList: { EmbedExtension },
  extensionUI: { EmbedFetcher },
};

export default Embed;
