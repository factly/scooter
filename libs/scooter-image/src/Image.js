import { Uploader } from "@factly/scooter-image";
import { ImageExtensionConfig } from "@factly/scooter-image";

export const Image = {
  extensionList: { ImageExtensionConfig },
  extensionUI: { Uploader },
};

export default Image;
