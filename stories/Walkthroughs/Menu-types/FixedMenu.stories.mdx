import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../lib";

<Meta
  title="Walkthroughs/MenuTypes/Fixed Menu"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Fixed Menu

The default scooterEditor layout comes with a set of always-on-top fixed menu
controls to interact with the editor.

<Canvas>
  <Story name="Fixed Menu">
    <Editor />
  </Story>
</Canvas>
