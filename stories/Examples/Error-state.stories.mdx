import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../lib";

<Meta
  title="Examples/Error state"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Error state

scooterEditor can be configured to provided a custom `error` prop which will be
rendered in the editor. The error prop can be a string, array of strings or an
object with a `message` property.

<Canvas>
  <Story name="Error state">
    <Editor
      error={{
        message: "Something went wrong in the editor",
      }}
    />
  </Story>
</Canvas>
